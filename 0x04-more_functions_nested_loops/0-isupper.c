#include"main.h"
/**
 * main - a function that checks for uppercase character
 * Return: Always 0
 */

int _isupper(int c)
{
	char upper;

	if (upper > 'A' && upper < 'Z')

		return (1);

	else

		return (0);
}
